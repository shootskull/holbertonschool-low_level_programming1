#include <stdio.h>
/**
 * main -hola mundo
 * a
 * Return: Always 0 (Success)
 */
int main(void)
{
int n;
char a;
for (n = 48; n <= 57; n++)
{
putchar(n);
}
for (a = 97; a <= 102; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
